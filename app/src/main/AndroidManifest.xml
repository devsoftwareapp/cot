<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- İnternet izni -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Depolama izinleri -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- Android 11+ için TÜM DOSYALARA ERİŞİM izni -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.PDFReader"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">
        
        <!-- ANA AKTİVİTE: MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- PDF dosyalarını açmak için -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/pdf" />
            </intent-filter>

        </activity>
        
        <!-- Diğer araç aktiviteleri -->
        <activity android:name=".BirlestirmeActivity" android:exported="false" />
        <activity android:name=".SesliOkumaActivity" android:exported="false" />
        <activity android:name=".OrcActivity" android:exported="false" />
        <activity android:name=".ImzaActivity" android:exported="false" />
        <activity android:name=".SikistirmaActivity" android:exported="false" />
        <activity android:name=".OrganizeActivity" android:exported="false" />
        <activity android:name=".ResimdenPdfActivity" android:exported="false" />
        <activity android:name=".PdfResmeActivity" android:exported="false" />
        
        <!-- PDF Görüntüleyici -->
        <activity android:name=".PdfViewerActivity" android:exported="false" />

    </application>

</manifest>
